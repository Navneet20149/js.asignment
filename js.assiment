// Define an NFT object
const nft = {
  name: "My Awesome NFT",
  eyeColor: "blue",
  shirtType: "hoodie",
  bling: true
};

// Print NFT details
function printNFTDetails(nft) {
  console.log("NFT Name:", nft.name);
  console.log("Eye Color:", nft.eyeColor);
  console.log("Shirt Type:", nft.shirtType);
  console.log("Bling:", nft.bling);
}

// Call the printNFTDetails function with the NFT object
printNFTDetails(nft);
